# 0.7 development review

Date: 2026-09-11. This records bounded development decisions and local candidate
certification. Remote release gates remain pending. Source identities below refer to local commits on
`feature/0.7-candidate`; generated reports remain ignored.

## Raw binding normalization

Accepted one internal ordered-list validation and `Binding::fromValue()` mapping
in `InputNormalizer`, called by `RawQuery` and `RawExpression`. Callers retain
their distinct list diagnostics and empty-SQL validation order. `CompiledQuery`
member/concrete-type precedence remains separate. Focused mixed-invalid-input
regressions and `composer check` passed; the public manifest is unchanged.

Compared the candidate containing this record against `f987aa0` using the same
current harness, PHP 8.4.24, SQLite 3.45.1, disabled Xdebug/PCOV instrumentation,
three warmups and nine samples. Five identical-source noise runs preceded both
source orders of `benchmarks/compare.php --suite=ci --allow-review`. All 36
scenario correctness digests matched. No actionable regression repeated in
both orders. Generated reports are under `benchmarks/results/normalization/`.

One-binding observer-off operations changed 1.301408 → 1.277627 ms in baseline-
first order and 1.293512 → 1.311145 ms in candidate-first order. Fifty-binding
observer-off operations changed 17.089096 → 17.650783 ms and 17.124738 →
17.287709 ms respectively. These are whole sampled workload times, not time per
query. The extraction removes duplicated rules; no speedup is claimed.

The host was not reserved exclusively for timing. Noise controls showed material
variance, including compile-only control movement; one-order hydration/result
signals did not reproduce. Final v0.6.0 certification must remeasure controls.

Reproduction: install the reference commit in a detached worktree, run
`benchmarks/noise.php --suite=ci --iterations=9 --warmups=3 --runs=5`, then
`benchmarks/compare.php --suite=ci --iterations=9 --warmups=3` with that
worktree's `--baseline-autoload`, the generated `--noise-report`, and each
`--source-order=baseline-first` / `--source-order=candidate-first`.

## Associative row validation

Disposition: retain local validation. Reviewed eager/first validation in
`Executor` and cursor validation in `Cursor`, plus `AssociativeHydrationTest`
and `CursorFailureTest`. Both reject non-array rows and numeric keys, but cursor
validation must construct cursor-context failures and participate in its own
fetch/finalization lifecycle. A shared helper would require passing an error
factory or contextual connection/query state on every row to eliminate two
small loops. The extraction does not sufficiently simplify these ownership
boundaries to justify new dispatch. No alternate implementation is retained and
no performance benefit is claimed.

The normalization comparison above exercised existing narrow/wide hydration,
first-row and cursor controls with matching digests. It is a characterization of
the retained implementation, not a benchmark of an unimplemented validator.
Existing focused hydration/cursor failure tests passed. Reopen only with a
concrete simpler design or new workload evidence; no new public validator exists.

## Affected-row reads and v0.6.0 comparison

Accepted reuse of the required affected-row result. Observed generated-ID
operations still acquire count metadata; unobserved generated-ID operations no
longer read it. Controlled PDO call counters prove one required read instead of
two and zero unobserved metadata reads. Required count failure still reports a
failed outcome after a write may have completed, with one cleanup attempt.
The existing batch scenario now includes a redundant-count direct PDO control.

A fresh five-run noise control and both full CI-suite source orders against
immutable v0.6.0 passed correctness parity with no repeated actionable timing
regression. Batch insert workload medians were 0.174080 → 0.180857 ms and
0.178987 → 0.324512 ms. Noise was substantial, including direct-PDO controls;
these results do not establish a speedup. Removing unnecessary calls is proven
by deterministic call-count tests, not inferred from these timings. Final
certification retains all samples and the one-order PDO-control signals.

Use the reproduction commands above with v0.6.0 as the baseline. The harness is
shared across sources, including the new control. Broader service and soak
qualification is recorded separately; this development result does not certify
transport modes or application throughput.

## Targeted failure coverage and mutation pilot

Added combined false bind/execute plus false/throwing cleanup cases. They assert
primary exception text, redaction, one outcome event, one cleanup attempt and
zero/one execute calls. Existing transaction, cursor, compiler and observer
seams remain the test boundary; no internal compiler/manager mocks were added.

An isolated worktree at `5a4d26b` plus these failure tests was mutated one fault
at a time, with original file bytes restored after each run. All eight faults
were detected by the named existing regressions:

| Injected fault | Detecting test |
| --- | --- |
| Drop a binding; reverse binding order | `ExpressionComparisonTest` exact ordered binding assertions |
| Reintroduce the raw argument hole | `MalformedConditionTest` rejection/non-attachment matrix |
| Drop callback context during inspection | `TransactionFailureTest` outer/nested exception identity |
| Omit cursor quarantine | `CursorFailureTest` post-failure execution rejection |
| Reintroduce double cleanup | `AssociativeHydrationTest` exact close counts |
| Reuse the original condition collection during copying | `QueryBuilderBehaviorTest` snapshot/SQL assertions |
| Let observer exceptions escape | SQLite `ExecutionTest` outcome non-interference |

No equivalent, unreachable or surviving fault occurred among these eight
selected mutations. This is not a repository-wide mutation score. Logs contain
synthetic fixtures only and remain local; source restoration was checked with
`git diff -- src` in the isolated worktree. The normal unmutated quality suite
passed afterward.

## Retained compatibility qualification

Completed on 2026-09-11 using PHP 8.4.24, mysqlnd 8.4.24, Docker 29.7.2 and
Compose 5.5.0. Source behavior is the candidate through `81b4784`; later changes
add test/packaging/qualification tooling. Both minimum and current matrices ran
behavior, execution and transaction probes for each direct/proxy target in all
four native/emulated × buffered/unbuffered modes. Each run produced 51 reports
and 781 observations, with zero failed observations. Observed-only fields are
characterizations, not additional guarantees. Disposable services were removed.

| Fixture | Resolved image digest |
| --- | --- |
| MariaDB 11.8.2 | `mariadb@sha256:2bcbaec92bd9d4f6591bc8103d3a8e6d0512ee2235506e47a2e129d190444405` |
| MySQL 8.0.11 | `mysql@sha256:ffa442557c7a350939d9cd531f77d6cbb98e868aeb4a328289e0e5469101c20e` |
| MariaDB 11.8.8 | `mariadb@sha256:24e76fcec8c003a0362d0dd53f4806e7e79458d7fdeaf47437760e19496f5a9c` |
| MySQL 8.0.46 | `mysql@sha256:7dcddc01f13bab2f15cde676d44d01f61fc9f99fe7785e86196dfc07d358ae2b` |
| ProxySQL 3.0.1 | `proxysql/proxysql@sha256:26da655f16cd135b67838530c8f59e2307736bfcc2f9def40b156725bdecc78f` |
| MaxScale 23.02.17-2 | `mariadb/maxscale@sha256:1806eb7cbed2249c15425047f42b4fadecde2732ed3493385d1095b403b9c516` |

Proxy binary output was `3.0.1-420-g2c26a42` and `MaxScale 23.02.17`.
Proxy results have matching direct MariaDB controls; configurations remain the
tracked synthetic fixtures. MySQL 8.4 and replacement proxies remain deferred.
No routing, failover or upstream-maintenance guarantee is added.

The maintained minimum-SQLite runner downloaded the official 3.39.2 source,
verified SHA-256 `852be8a6183a17ba47cee0bbff7400b7aa5affd283bf3beefc34fcd088a239de`,
built with `-O2 -DSQLITE_ENABLE_COLUMN_METADATA`, and verified `3.39.2` through
actual PDO. SQLite integration passed 41 tests / 237 assertions, as did behavior,
execution and transaction probes, including file-backed contention. The initial
build without column metadata could not load this host's PDO extension; the
maintained runner includes the required build flag and fails on runtime mismatch.
Current SQLite 3.45.1 also passed. This does not substitute for PHP 8.2 runtime CI.

LOB characterization in these exact configurations returned the remaining
`cdef` bytes, an empty repeated payload and `abcdef` after caller rewind.
Non-seekable socket streams worked. Closed-after-binding and stream-context
resources produced `QueryExecutionException` retaining `PDOException`; resources
closed before binding were rejected at construction. No type restriction or
portable promise about future PDO exception shapes is introduced.

Reproduce with `MARIADB_VERSION=11.8.2 MYSQL_VERSION=8.0.11 bash
tools/database-probes/run-services.sh`, `RUN_BENCHMARKS=true bash
tools/database-probes/run-services.sh`, and
`bash tools/database-probes/run-minimum-sqlite.sh`. Keep minimum reports before
rerunning the current matrix because top-level generated probe reports are
replaced. Image/version records live in the generated `environment/` directory.
Current live benchmarks also passed for all four targets in both native buffer
modes, alongside four concurrent four-scenario soak workers. Detailed retained
memory/descriptor review belongs to candidate certification below.

## Local candidate certification

The clean detached checkout `6a9cc1927c32c66eac7d8f92ea186ac96e56d972`
passed strict Composer validation, dependency audit (no advisories or abandoned
packages), `composer check`, line/branch coverage and `composer package:check`.
PHP 8.4.24 / SQLite 3.45.1 ran 625 tests with 1,845 assertions. Final artifact
review then added a regression for ZIP directory entries marked as symbolic
links and moved link rejection ahead of directory skipping; the full quality
suite passed 626 tests / 1,849 assertions. This final tooling correction does
not change library source or the measured database operations.

| Coverage gate | Observed | Required |
| --- | --- | --- |
| Overall line | 95.61% | 90% |
| Compiler line | 97.00% | 95% |
| Overall branch | 92.27% | 80% |
| Compiler branch | 92.04% | 90% |

Coverage used the maintained PCOV line and Xdebug branch/path commands. The
public API manifest, external static consumer, PHPStan level 9, style, lint,
platform requirements, executable examples, documentation links and repository
checks passed. Source consistency also passed with the independently verified
published-version input `0.6.0`; the immutable tag resolves to
`6a9b22ea827888aac3818d7ea5ddddde01a66f11`.

The clean Composer and Git archives each contained 163 files / 553,661
uncompressed bytes; each independent no-dev installed consumer passed. The
contaminated-source archive had identical contents and the same SHA-256 as the
clean Composer archive. Checksums for this exact pre-certification-documentation
commit were:

| Archive | SHA-256 |
| --- | --- |
| Composer / contaminated Composer | `95574cd105de022060ac6962343e81a985bce5021ab6b72db8041e43ca172788` |
| Git | `2a65dae7585d2fb8e29f3ebb81d62f3afb171660a8d7f2854e3cb192da2f4340` |

These hashes identify that source snapshot, not future release artifacts.
Repeat `composer package:check` after the certification commit and archive its
clean-checkout report alongside the eventual CI artifacts. The checker reports
its own checkout identity; supplied hosted archives still need independent
source-reference verification.

The 36-scenario CI comparisons described above used immutable `v0.6.0`, five
fresh identical-source noise runs, both source orders, three warmups and nine
samples. Correctness parity passed in both orders. Only `pdo_control_5000/pdo`
required timing review in baseline-first order, and only
`pdo_control_1000/pdo` in candidate-first order; neither repeated. This noisy
shared host establishes no throughput improvement. The decision rests on
preserved correctness, deterministic removed-call counts and no repeatable
regression under the maintained review policy.

The reference soak on clean `6a9cc19` used two warmups and seven samples across
four scenarios / five operations. Retained allocation growth and descriptor
deltas were zero in every operation. Maximum operation-worker RSS was
44,802,048 bytes; maximum transient used allocation was 1,336,192 bytes. Four
concurrent CI-profile soak workers during live fixture qualification also showed
zero retained allocation and descriptor growth across all twenty operations;
maximum RSS was 42,090,496 bytes. These are bounded synthetic observations,
not a claim that memory never grows in application workloads. The maintained
soak limit passed; all eight native buffered/unbuffered direct/proxy benchmark
reports passed their correctness gates. Minimum/current execution, transaction,
LOB and SQLite contention evidence is recorded above.

### Contract, security and tooling review

Reviewed the complete library diff from `v0.6.0` against the
[security checklist](../maintainers/security-review-checklist.md). Identifiers,
trusted raw syntax and bound values remain separate; exact ordered bindings,
child snapshots and historical partial builder mutation remain tested.
Signatures, PHP/database floors and accepted resource varieties are unchanged.
Generated IDs remain on the same connection; transaction quarantine, external
ownership and observer non-interference retain their existing boundaries.
No reconnect, retry, replay or hidden LOB materialization was added. The eight
restored mutation faults and controlled combined failures provide focused
negative evidence; they are not a comprehensive mutation score.

Binding values are not interpolated by the library. Raw SQL literals and
retained driver/application exception chains can nevertheless contain private
data. Current public API and security guidance now state that distinction;
the stronger historical `0.6.0` certification wording is not a sanitization
guarantee and has not been silently rewritten. Synthetic probes and staged
source/documentation diffs were reviewed for credentials and private fixtures.
Archive review additionally rejects directory-shaped symbolic links before
extraction. Public testing utilities and useful package documentation remain
available to installed consumers.

Tooling used Composer 2.9.1, PHPUnit 11.5.56, PHPStan 2.2.8,
PHP_CodeSniffer 4.0.4 and Git 2.43.0. Dependency constraints and the resolved
local development installation passed strict validation and audit. Existing
workflow actions remain pinned to reviewed commit references; new lanes reuse
those references. No unrelated dependency or tool refresh was introduced.
The audit result is a dated dependency check, not a guarantee about all tooling
or all future advisories.

### Pending release evidence

Local implementation and certification checks have passed. The candidate is
still an unpushed feature branch. A read-only remote lookup on 2026-09-11 found
the default branch at `v0.6.0` and no published candidate branch. Consequently,
matching hosted-source provenance and required remote CI have no candidate
result to review. Local PHP 8.4 evidence does not replace the required PHP
8.2–8.5 CI matrix, lowest-dependency lane or scheduled service jobs. The actual
SQLite 3.39.2 local run used PHP 8.4; its new PHP 8.2 CI lane remains unrun.
These required gates remain unchecked in the single
[delivery tracker](../plans/0.7.md).

Follow the [release process](../maintainers/release-process.md) when external
actions are separately authorized. Keep `0.6.0` as the current published
version until publication; `0.7` changes remain under `[Unreleased]`.
No release branch, push, PR, tag or publication was performed. Final published
dist resolution and plan retirement remain post-publication work.
